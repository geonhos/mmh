import type { RoomConfig } from '../types';

export const DEFAULT_ROOM: RoomConfig = {
  width: 5,
  depth: 4,
  height: 2.8,
};

export const COLORS = {
  wall: '#e8e0d8',
  floor: '#c4a882',
  wallOpacity: 0.3,
};
